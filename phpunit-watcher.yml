watch:
  directories:
    - src
    - tests
  fileMask: '*.php'
notifications:
  passingTests: false
  failingTests: false
phpunit:
  binaryPath: phpunit
  arguments: -c /dev/null --stop-on-failure --bootstrap ./vendor/autoload.php
  timeout: 180
